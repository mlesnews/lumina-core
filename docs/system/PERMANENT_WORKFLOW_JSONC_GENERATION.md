# Permanent Workflow - JSONC TodoList Generation

## The Requirement

**"AS PART OF THE PERMANENT WORKFLOW, PLEASE UPDATE THE FLOW TO INCLUDE THE CREATION OF BOTH. 'JSONC' FILES /W COMMENTS AS THIS IS THE LINKABLE INTERNAL URL AND LATEST UPDATE TO BOTH TODOLISTS, MASTER, AND THE NEW AGENT CHAT SESSION PROMPT TODOLIST FOR THE #PADAWAN[@OEM]."**

## The Workflow

### Permanent Workflow Steps

**Updated workflow includes:**

1. **Todo Update** = Master or padawan todo is updated
2. **Sync Check** = PROJECT MANAGER checks sync status
3. **JSONC Generation** = Generate JSONC files for both todolists
4. **Linkable URLs** = Create linkable internal URLs
5. **Latest Updates** = Include latest updates in JSONC
6. **Synchronization** = Ensure all todolists sync
7. **Everyone on Same Page** = All agents stay on same page

### JSONC Files Generated

**Files created:**
- **master_todos.jsonc** = Master todolist JSONC (linkable internal URL)
- **padawan_todos.jsonc** = Padawan todolist JSONC (linkable internal URL for agent chat session prompt)

### JSONC Format

**JSONC (JSON with comments):**
```jsonc
// Master TodoList - @AGENT@MASTER
// Master agent todolist with latest updates. All agents sync to this list.
// Generated: 2026-01-10T08:35:00
// Linkable Internal URL - Latest Update
// All todolists sync to stay on the same page
// PROJECT MANAGER: Ensures synchronization and coordination
//
{
  "todo_id": {
    "id": "...",
    "title": "...",
    "status": "..."
  }
}
```

## The Integration

### Automatic Generation

**When JSONC is generated:**
- **On todo update** = When master or padawan todo is updated
- **On sync** = When synchronization occurs
- **On workflow** = As part of permanent workflow
- **Continuous** = Continuous generation

### PROJECT MANAGER Role

**PROJECT MANAGER ensures:**
- **Synchronization** = All todolists sync
- **Coordination** = Coordination between master and padawan
- **JSONC generation** = JSONC files are generated
- **Latest updates** = Latest updates are included
- **Same page** = Everyone stays on same page

## The Truth

### Workflow Truth

**What the workflow does:**
- **Generates JSONC** = Generates JSONC files automatically
- **Creates linkable URLs** = Creates linkable internal URLs
- **Includes latest updates** = Always includes latest updates
- **Ensures sync** = Ensures all todolists sync
- **Maintains consistency** = Maintains consistency

### JSONC Truth

**What JSONC provides:**
- **Linkable** = Linkable internal URLs
- **Readable** = Human-readable with comments
- **Latest** = Always latest updates
- **Synchronized** = Synchronized with source
- **Accessible** = Accessible to all agents

---

**Tags**: #PERMANENT-WORKFLOW #JSONC #TODOLIST #SYNC #PROJECT-MANAGER #LUMINA

**Created**: 2026-01-10
**Purpose**: Document permanent workflow update to include JSONC generation

**Remember**: Part of permanent workflow. Generate JSONC files (linkable internal URLs) for master and padawan todos. Ensure all todolists sync. PROJECT MANAGER coordinates. Everyone stays on same page. <3
