# PROJECT MANAGER Role & Responsibilities

## The Role

**PROJECT MANAGER** = Ensures synchronization and coordination of all todolists

## The Responsibility

**"This is the role/responsibilities of the 'PROJECT MANAGER.'"**

### Core Responsibilities

**Synchronization:**
- **Sync all todolists** = Ensure all todolists sync so everyone stays on the same page
- **Master todolist** = Keep master todolist synchronized
- **Padawan todolist** = Keep padawan todolist synchronized
- **Agent chat session prompt todolist** = Keep agent chat session prompt todolist synchronized
- **Consistency** = Ensure consistency across all todolists

### JSONC File Generation

**Generate JSONC files:**
- **JSONC format** = JSON with comments (linkable internal URLs)
- **Master todolist JSONC** = Generate `master_todos.jsonc` with latest updates
- **Padawan todolist JSONC** = Generate `padawan_todos.jsonc` with latest updates
- **Linkable URLs** = JSONC files are linkable internal URLs
- **Latest updates** = Always include latest updates in JSONC files

### Permanent Workflow

**Part of permanent workflow:**
- **Automatic generation** = Automatically generate JSONC files
- **On todo update** = Generate JSONC when todos are updated
- **On sync** = Generate JSONC when syncing
- **Continuous** = Continuous JSONC generation as part of workflow

## The System

### JSONC Files

**What JSONC files contain:**
- **Comments** = JSON with comments (// style comments)
- **Latest updates** = Always latest updates
- **Linkable** = Linkable internal URLs
- **Synchronized** = Synchronized with source JSON files
- **Readable** = Human-readable with comments

### Synchronization

**How synchronization works:**
- **Source files** = Master and padawan JSON files are source of truth
- **JSONC generation** = Generate JSONC from source files
- **Sync status** = Track sync status
- **Consistency** = Ensure consistency across all files
- **Everyone on same page** = All agents stay on same page

## The Assignment

### PROJECT MANAGER Assignment

**Assigned to:**
- **@PROJECT-MANAGER** = Assigned as PROJECT MANAGER
- **Role** = PROJECT MANAGER
- **Responsibility** = Synchronization and coordination
- **Tasks** = Generate JSONC files, ensure sync

### Padawan Todo

**Padawan task:**
- **Generate JSONC files** = Generate JSONC for master and padawan todos
- **Ensure sync** = Ensure all todolists sync
- **Linkable URLs** = Create linkable internal URLs
- **Latest updates** = Include latest updates
- **Stay on same page** = Keep everyone on same page

## The Truth

### PROJECT MANAGER Truth

**What PROJECT MANAGER does:**
- **Synchronizes** = Synchronizes all todolists
- **Coordinates** = Coordinates between master and padawan
- **Generates** = Generates JSONC files
- **Maintains** = Maintains consistency
- **Ensures** = Ensures everyone stays on same page

### JSONC Truth

**What JSONC files provide:**
- **Linkable** = Linkable internal URLs
- **Readable** = Human-readable with comments
- **Latest** = Always latest updates
- **Synchronized** = Synchronized with source
- **Accessible** = Accessible to all agents

---

**Tags**: #PROJECT-MANAGER #JSONC #TODOLIST #SYNC #COORDINATION #LUMINA

**Created**: 2026-01-10
**Purpose**: Document PROJECT MANAGER role and JSONC todolist generation workflow

**Remember**: PROJECT MANAGER ensures all todolists sync so everyone stays on the same page. Generate JSONC files (linkable internal URLs) for master and padawan todos. Part of permanent workflow. <3
